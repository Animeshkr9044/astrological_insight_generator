# Astrological Insight Generator - Environment Configuration

# ============================================================================
# LLM Configuration
# ============================================================================

# OpenAI API Key (required for OpenAI provider)
# Get your key from: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# LLM Provider: "openai" (requires API key) or "mock" (no API key needed)
LLM_PROVIDER=openai

# OpenAI Model (gpt-4o-mini is recommended for cost-effectiveness)
OPENAI_MODEL=gpt-4o-mini

# LLM Generation Parameters
LLM_MAX_TOKENS=150
LLM_TEMPERATURE=0.7

# ============================================================================
# Vector Store Configuration (RAG)
# ============================================================================

# Enable/disable vector store for RAG
VECTOR_STORE_ENABLED=true

# Mode: "memory" (fast, in-memory) or "server" (persistent, scalable)
VECTOR_STORE_MODE=memory

# Embedding model for semantic search
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Qdrant collection name
VECTOR_COLLECTION_NAME=astrological_knowledge

# Server mode configuration (only needed if VECTOR_STORE_MODE=server)
# QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=

# ============================================================================
# Server Configuration
# ============================================================================

# Server host
HOST=0.0.0.0

# Server port
PORT=8000

# Debug mode (enables auto-reload and verbose logging)
DEBUG=false

# ============================================================================
# Translation Configuration
# ============================================================================

# Enable translation (currently stubbed)
TRANSLATION_ENABLED=false

# Use mock translation (for testing)
TRANSLATION_MOCK=false

# ============================================================================
# Other Settings
# ============================================================================

# Application name
APP_NAME=Astrological Insight Generator

# API version
API_VERSION=v1

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Caching settings (for future use)
CACHE_ENABLED=false
CACHE_TTL=86400

# ============================================================================
# Quick Start Tips
# ============================================================================

# Option 1: Use OpenAI (Recommended for production)
# 1. Replace 'your_openai_api_key_here' with your actual OpenAI API key
# 2. Keep LLM_PROVIDER=openai
# 3. Run: ./setup.sh

# Option 2: Use Mock Provider (Free testing, no API key needed)
# 1. Change LLM_PROVIDER=mock
# 2. Run: ./setup.sh
# Note: Mock provider generates pre-defined responses for testing

# Option 3: Use Qdrant Server (Production deployment)
# 1. Start Qdrant: docker run -p 6333:6333 qdrant/qdrant
# 2. Set VECTOR_STORE_MODE=server
# 3. Set QDRANT_URL=http://localhost:6333
# 4. Run: ./setup.sh

